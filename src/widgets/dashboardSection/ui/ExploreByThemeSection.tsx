import SectionWrapper from "./SectionWrapper";

export default function ExploreByThemeSection() {
  return (
    <SectionWrapper title="테마별 보기">
      <div className="text-gray-400 text-sm">
        {/* 테마별 보기 콘텐츠가 여기에 표시됩니다. */}
      </div>
    </SectionWrapper>
  );
}
