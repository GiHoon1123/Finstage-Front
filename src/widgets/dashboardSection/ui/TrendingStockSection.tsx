import SectionWrapper from "./SectionWrapper";

export default function TrendingStockSection() {
  return (
    <SectionWrapper title="인기Stock">
      <div className="text-gray-400 text-sm">
        {/* 인기Stock 콘텐츠가 여기에 표시됩니다. */}
      </div>
    </SectionWrapper>
  );
}
